# Cloud sync is OPTIONAL. Leave these unset and Getsu runs as a fully
# functional, account-free, local-first app (the free tier).
#
# To enable cloud sync (the paid tier), create a Supabase project and set:
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
